<Window x:Class="WeatherTool.MVVM.View.WeatherView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherTool.MVVM.View"
        xmlns:vm="clr-namespace:WeatherTool.MVVM.ViewModel"
        mc:Ignorable="d"
        Title="Weather" Width="400"
        SizeToContent="Height">
    <Window.DataContext>
        <vm:WeatherViewModel></vm:WeatherViewModel>
    </Window.DataContext>
    <Window.Resources>
        <local:DoubleConverter x:Key="doubleConverter"></local:DoubleConverter>
    </Window.Resources>
    <Border BorderThickness="10">
        <StackPanel>
            <DockPanel HorizontalAlignment="Stretch">
                <TextBlock Text="Date"/>
                <TextBlock Text="Min"/>
                <TextBlock Text="Max"/>
                <TextBlock Text="Average"/>
            </DockPanel>
            <DockPanel>
                <TextBox Text="{Binding WeatherDay.Date, StringFormat=dd.MM.yyyy}"></TextBox>
                <TextBox Text="{Binding WeatherDay.LowTemp, Converter={StaticResource doubleConverter}, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                <TextBox Text="{Binding WeatherDay.HighTemp, Converter={StaticResource doubleConverter}, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                <TextBox Text="{Binding WeatherDay.AverageTemp, Mode=OneWay}"></TextBox>
            </DockPanel>

        </StackPanel>
    </Border>
</Window>
